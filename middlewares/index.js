const middleware1 = require('./middleware1.js')
const middleware2 = require('./middleware2.js')
const middleware3 = require('./middleware3.js')

module.exports = {
  middleware1,
  middleware2,
  middleware3
}
